# This file is required to mark the config directory as a Python package.
# It may be empty, or it may contain configuration settings or other code that should be used throughout the app.
# In this case, the app package imports the settings module from the config package to access configuration variables.
